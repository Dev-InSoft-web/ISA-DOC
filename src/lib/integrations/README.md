# Integrations

Adaptadores a servicios o modos de despliegue externos.

- `mermaid/` — render de diagramas en docs y tickets.
- `huggingface/` — remove-bg (build tickets).
- `postman/` — store de colecciones para Socket.IO / PostmanPanel.
- `runtime/` — `staticMode` (build GitHub Pages vs dev).
