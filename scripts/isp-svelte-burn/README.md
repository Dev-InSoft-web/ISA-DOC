# isp-svelte-burn

Quema `Card.old.svelte` (imports al paquete npm) en `ISP-SvelteComponents/.../Card.svelte` con esbuild + ofuscación.

```bash
node scripts/isp-svelte-burn/burn-card.mjs
```

Sincronizar paquete en consumidores: `scripts/PS1/sync-local-isp-clientesis.ps1`.
