# _shared

| Módulo | Uso |
| --- | --- |
| `isa-doc-root.ts` / `.mjs` | `ISA_DOC_ROOT` — raíz del repo desde cualquier script bajo `scripts/` |

Importar con profundidad relativa (`../../_shared/…` desde `patyia/prompts/`, etc.).
