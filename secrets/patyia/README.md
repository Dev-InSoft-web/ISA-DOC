# Secretos Paty IA (ISA-DOC — solo cliente front)

| Archivo | Descripción |
|---------|-------------|
| `lab-client.env` | URL de **lab-langgraph** (`LAB_LANGGRAPH_URL` → build `PUBLIC_LAB_LANGGRAPH_URL`) |
| `lab-client.env.example` | Plantilla |

Runtime backend (API keys, `DATABASE_URL`, tokens, Postman, prompts operativos): repo **lab-langgraph** (`secrets/`, `data/`, `local.settings.json`).

Ver `lab-langgraph/docs/ARCHITECTURE-BACK-FRONT.md`.
