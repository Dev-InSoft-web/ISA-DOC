# publish — gh-pages / static-api

| Script | npm |
| --- | --- |
| `snapshot-data.mts` | `snapshot:gh-pages` |

Genera `public/static-api/*` (tablas, fragments, postman, `patyia/prompts/comparativa.json`). Usa `patyia/prompts/build-paty-prompts-json.mts`.

Publicación completa: `npm run pub:gh-pages` (`scripts/PS1/pub-isa-doc/`).
