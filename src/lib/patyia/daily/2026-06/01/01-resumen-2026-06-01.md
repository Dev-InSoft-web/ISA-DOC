## 2026-06-01 — Resumen de trabajo (PatyIA)

- **Contexto:** Cierre y diligencia de **TK-1431163** (composición OpenAI: conservar `PR_GENERAL` + instrucciones por tipo). Corrección de fallo de deploy en CI por dependencia compile-time de `openai-pricing.json`.

- **Qué se hizo:**
  - Se validó la solución ya aplicada en `OpenIAServer.ts`: instrucciones por tipo vía `prompt.variables.instrucciones_tipo` (sin `instructions` en el body que sobrescribía el template).
  - Se diligenció el ticket en ISA-DOC con commits, tiempos (2 h) e ins/del.
  - Se corrigió `UlMetrics.ts` para cargar `openai-pricing.json` en runtime con fallback a costos en cero si el archivo no existe (gitignored); el build/deploy ya no depende de ese JSON.
  - Se eliminaron `TGroqController.ts` y `POST-Groq.ts` (código de prueba no usado en producción).
  - Se mapeó el repo `PatyIA` → `ISS-AyudasCPIA` en las URLs de commits del visor de tickets.

- **Archivos modificados (ISS-AyudasCPIA / PatyIA):**
  - `src/020 Controller/tools/UlMetrics.ts`
  - `.gitignore` (entrada obsoleta de POST-Groq)
  - Eliminados: `src/020 Controller/tools/TGroqController.ts`, `src/functions/POST-Groq.ts`

- **Archivos modificados (ISA-DOC):**
  - `src/lib/tickets/TK-1431163.ts`, `src/lib/tickets/index.ts`
  - `src/lib/tickets/template.ts` (alias repo PatyIA → ISS-AyudasCPIA)

- **Commits relacionados (ISS-AyudasCPIA):**
  - `a9808b7` · feat: prompts IA; `instrucciones_tipo` en variables del template
  - `d4e3f75`, `8a5bee3` · refactors UlMetrics / OpenIAServer
  - `d8bf73b` · refactor(TK-1431163): elimina Groq y ajusta carga de precios en UlMetrics

- **Estado:** TK-1431163 diligenciado. Deploy desbloqueado tras fix de UlMetrics. Pendientes menores documentados en bitácora 28-may: fallback de `nombre_usuario` sin JWT y persistencia de `itdconsulta` en conversación.

---

`Registrado por:` Jeff-Aporta
