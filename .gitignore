node_modules/
dist/
dist-gh-pages/
.gh-pages-skip/
.astro/
.env
public/static-api/
public/patyia/openai/images/

# secretos (NUNCA versionar)
.agent/
*.npmrc

# carpeta de backups (local.settings, npmrc, etc.)
backups/

# JWT tokens (NUNCA versionar — uno por aplicativo)
token.json
token.patyia.json
token.clientesis.json
