# ISA-DOC → solo cliente de lab-langgraph (NO poner API keys ni MSSQL aquí)
LAB_LANGGRAPH_URL=http://127.0.0.1:5500
# JWT de POST /api/auth/token (válido ~30 días). El front lo pide con el modal de auth.
LAB_AUTH_TOKEN=
# MSSQL: consultas GET/POST /api/mssql/{target}/query (sin auth); exec requiere JWT en el navegador.
