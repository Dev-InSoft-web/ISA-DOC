## 2026-06-01 — Resumen de trabajo

- **Contexto:** Continuación y cierre del diagnóstico y corrección de TK-1430974 (Plan de estudio → Cursos integrados: creación desde vista grande no persiste). Se trabajó en UI, controlador y documentación asociada en ISA‑DOC.

- **Qué se hizo:**
  - Se aisló la causa: el `submit` del formulario hijo era capturado por el form padre y la decisión de crear/modificar venía del parámetro global `itdForm` en la URL. Se cambió la lógica para decidir create/modify en función del modelo y no de `itdForm`.
  - Se implementó una acción Svelte `interceptNestedSubmit` que previene que el submit del hijo sea absorbido por el padre y delega en `submitCurso()`.
  - Se añadió `normalizeItem(item)` en el controlador `PlanDeEstudio` para asegurar campos obligatorios antes de persistir (ej. `iplanestudio`, `qorden`).
  - UX: `BtnRefAutoOpen` ahora abre automáticamente cuando el `value` está vacío (salvo en modo `view`).
  - La grilla se refresca tras creación con `catalogoCursos.refreshGrid()` para mostrar la fila nueva inmediatamente.
  - Documentación: se actualizó la bitácora en ISA‑DOC con el detalle del diagnóstico y la evidencia; se agrupó Mayo en un acordeón.

- **Archivos modificados (resumen):**
  - [src/components/views/contapymeu/capacitacion/plandeestudio/_Details/CtlgoCursosDePlan.svelte](src/components/views/contapymeu/capacitacion/plandeestudio/_Details/CtlgoCursosDePlan.svelte)
  - [src/components/views/contapymeu/capacitacion/_comps/especial/BtnRefAutoOpen.svelte](src/components/views/contapymeu/capacitacion/_comps/especial/BtnRefAutoOpen.svelte)
  - [src/lib/ContaPymeU/2.Capacitacion/PlanDeEstudio.ts](src/lib/ContaPymeU/2.Capacitacion/PlanDeEstudio.ts)
  - ISA‑DOC: [src/components/panels/BitacoraPanel.svelte](src/components/panels/BitacoraPanel.svelte)
  - ISA‑DOC: [src/lib/bitacora/daily/2026-05/29/01-qa-camilo-itdform-create.md](src/lib/bitacora/daily/2026-05/29/01-qa-camilo-itdform-create.md) (documentación relacionada añadida)

- **Commits locales relevantes:**
  - `869c5ce` · cambios en ISW (Cursos integrados: interceptNestedSubmit + submitCurso)
  - `ac1e457` · PlanDeEstudio: normalizeItem y ActCrear/ActModificar
  - `16b8db9` · BtnRefAutoOpen: auto-open cuando value vacío
  - `455efba` · ISA‑DOC: docs(bitacora): se registra resolución de TK-1430974 y evidencia
  - `f3de113` · ISA‑DOC: docs(bitacora): agrupa mayo y completa TK-1430974 con HTML, evidencia y tiempos

- **Evidencia visual:**
  - Planeado: generar capturas Playwright y guardarlas en `src/lib/tickets/assets/TK-1430974/` con los nombres `tk1430974-dialog.png`, `tk1430974-picker.png`, `tk1430974-after-grid.png`.
  - Nota: la primera ejecución de Playwright falló por invocación sin `pageId`; se re-ejecutará correctamente y luego se correrá `npm run assets:upload` para subir a imgbb y actualizar `imgbb-map.json`.

- **Pruebas realizadas:**
  - Prueba manual: antesCount 9 → despuésCount 10 (la grilla se refresca y la fila aparece).

- **Siguientes pasos:**
  1. Ejecutar Playwright correctamente (usar la página abierta en el navegador), guardar las capturas en `src/lib/tickets/assets/TK-1430974/`.
  2. Ejecutar `npm run assets:upload` para subir imágenes a imgbb y actualizar referencias en la MD.
  3. Actualizar el MD del ticket para usar las imágenes subidas y commitear cambios (autocommit permitido en ISA‑DOC).

---

`Registrado por:` Jeff-Aporta / Copilot
