# Conexión a BD (MSSQL) — copia este archivo a `.env` y rellena los valores.
# Mismas variables que usa el ISS en `local.settings.json`.
hostdb=mssql2.insoft.co
portdb=51433
userdb=SERVER-CLIENTES
passdb=
namedb=CLIENTES
